<form method="post" action="{{ path('app_machin_delete', {'id': machin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ machin.id) }}">
    <button class="btn">Delete</button>
</form>
